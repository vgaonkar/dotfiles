# Secrets Configuration
# Copy this file to secrets.yml and fill in your actual values
# DO NOT commit secrets.yml to git - it's already in .gitignore

# Git configuration
git:
  user_name: "Your Name"
  user_email: "your.email@example.com"

  # Work identity
  work_email: "your.work@company.com"

  # GPG signing keys (run `gpg --list-secret-keys --keyid-format LONG`)
  signing_key: "YOUR_PERSONAL_GPG_KEY_ID"
  work_signing_key: "YOUR_WORK_GPG_KEY_ID"

# SSH configuration
ssh:
  # Work SSH jump/bastion host alias (used in ~/.ssh/config)
  work_host: "work"
  work_hostname: "work-server.example.com"

  # Personal servers
  personal_hostname: "home-server.example.com"
  personal_user: "your-personal-username"

# API keys (examples)
# aws:
#   access_key_id: "YOUR_ACCESS_KEY"
#   secret_access_key: "YOUR_SECRET_KEY"
#
# github:
#   token: "ghp_YOUR_TOKEN"
#
# openai:
#   api_key: "sk-YOUR_API_KEY"

# Work-specific
work:
  vpn_endpoint: "vpn.company.com"
  proxy_url: "http://proxy.company.com:8080"
