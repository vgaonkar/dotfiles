# Site

Open `index.html` in a browser to preview the dotfiles showcase site.

For local development with proper relative paths:
```
python3 -m http.server 8080 --directory ..
```
Then visit: http://localhost:8080/site/
